# Build the module.
steps: 
- name: 'docker.io/hashicorp/terraform:0.12.26'
  id: 'plan-sandbox'
  entrypoint: /bin/sh
  args: 
    - 'terraform init'
    - 'terraform validate'
    - 'terraform plan'
  dir: 'environments/sandbox'